name: Deploy

on:
  push:
    tags:
      - 'v*'  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Create zenodo_token file
      run: echo "ACCESS_TOKEN-sandbox: CNXNk6GBg0dQUvZJsCUEajG4ZZnAr8YUrWPR9oCucnu8vq39jFJDUGoYW6WK" > zenodo_token

    - name: Set up Python
      uses: actions/setup-python@v2

    - name: install req
      run: pip install -r zenodo/req.txt 

    - name: Run script
      run: python zenodo/test.py 
      env:
        VERSION_TAG: ${{ github.event.release.tag_name }}


      

        